<scroll-view class="containers" scroll-y="true" scroll-with-animation="true" style="height: 100vh;">
<view class="bgb" ></view>
  <image src="/img/home_bg@1x.png" mode="aspectFill" class="bg"/>
<view class="headtext">首页</view>
<view class="niu"><image src="/img/SZTU-E会约.svg" mode=""/></view>
<view class="xiao">轻松预约校园会议室</view>
  <!-- <scroll-view scroll-x="true" scroll-with-animation="true" style="width: {{timelen*25}}vw;" scroll-into-view="today"> -->
  <view class="dates">
    <view class="yi">
      <view class="zzz">预约日期</view>
      <view class="yyy">{{today}}</view>
    </view>
    <view style="display: flex; margin-top: 8px; justify-content: space-around;">
    <view bind:tap="changeFlag" wx:for="{{time_items}}" class="time-item {{item.flag?'ttt':''}}" wx:key="*this" data-index="{{index}}" id="{{item.flag?'today':''}}">
      <view class="zou" style="color:{{item.flag?'':'#5A5A5A'}}">{{item.zou}}</view>
      <view class="date" style="color:{{item.flag?'':'#5A5A5A'}}">{{item.date}}</view>
      <view class="kong" style="color:{{item.flag?'':'#A3A3A3'}}">有空闲</view>
      <view class="yuan" ></view>
    </view></view>
    </view>
  <!-- </scroll-view> -->
  <custom-loading show="{{lolo}}" title="加载中..."></custom-loading>
  <view class="chico">
      <view class="ch1">
        <view class="konw"></view>
        <view class="ksk">预约会议室</view>
      </view>
      <view class="ch2">
      <block wx:for="{{items}}">
        <view  class="item {{item.act?'active':''}}" wx:key="*this" data-index="{{index}}" bind:tap="changeRoom">
      <view class="name" style="color: {{item.act?'#FFFFFF':''}};">{{item.name}}</view>
      <view class="num" style="color: {{item.act?'#FFFFFF':''}};">（{{item.num}}人）</view>
      <image class="acimg" src="{{item.act?'/img/路径 3865.svg':''}}" mode=""/>
    </view> 
  </block>
      </view>
  </view>
  <view class="kuang">
  <view style=" display: flex; justify-content: center;">
      <view class="ks">
        <view bind:tap="choices"  wx:for="{{kkk}}" class="kkk {{item.flag?'kact':''}}"  wx:key="*this" data-index="{{index}}" style="color: {{item.ed==item.ke ?'#A8A8A8':''}}; background: {{item.ed==item.ke ?'#F5F5F5':''}};">
          <view class="tst">{{item.time}}</view>
          <view class="tst" >{{item.ed==item.ke ?'不可预约':'空闲'}}</view>
        </view>
      </view>   
      </view> <view style="display: flex; justify-content: center;">
      <view class="qer">预约</view></view>
      </view>  
  <!-- <view wx:if="{{tab1}}" class="hui">
    <view bind:tap="navigate" data-url="/pages/order1/order1" wx:for="{{items}}" class="item " style="background-color:{{item.ed==item.ke? '#FAFAFA':''}};" wx:key="*this" data-index="{{index}}">
      <view style="display: flex; justify-content: space-around; align-items: center; width: 90%;margin-left: 8%;margin-top: 5px;">
        <view class="docu">
          <image src="{{item.ed==item.ke ?'/img/Paper.svg':'/img/Paper@1x.png'}}" mode="" />
        </view>
        <view class="xuan {{item.ed==item.ke ? 'man':''}}">{{item.ed==item.ke ?'已满':item.ke+'/'+item.ed}}</view>
      </view>
      <view class="name">{{item.name}}</view>
      <view class="num">人数{{item.num}}</view>
    </view>
  </view> -->
  <!-- <view wx:if="{{tab2}}" class="time">
    <view class="chan">选择时段</view>
    <view class="kuang">
      <view class="ks">
        <view bind:tap="choices" data-url="/pages/order2/order2" wx:for="{{kkk}}" class="kkk"  wx:key="*this" data-index="{{index}}">
          <view class="tst">{{item.time}}</view>
          <view class="sts" style="background-color: {{item.ed==item.ke ?'rgba(208, 206, 206, 0.41)':''}};">{{item.ed==item.ke ?'约满':item.ke+'/'+item.ed}}</view>
          <view class="gou"><image src="{{item.flag ?'/img/Severicons (1).svg':'/img/Severicons.svg'}}" mode=""/></view>
        </view>
      </view>
    </view>
    <view style="display: flex; align-items: center; justify-content: center;">
  <view bind:tap="navigate" data-url="/pages/order2/order2" class="qur">
  确认
  </view></view>
  </view> -->
</scroll-view>